# Notes Frontend - Environment

The frontend reads the backend API base URL from `REACT_APP_API_BASE`.

- Local development default (see `.env`): `http://localhost:3001`
- In cloud/preview deployments, set this to the deployed backend URL.

Example configuration is available in `.env.example`.
